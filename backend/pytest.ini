[pytest]
testpaths = tests
addopts = -ra -q --strict-markers
markers =
    integration: tests que tocan servicios externos (skip en CI standard)
    slow: tests > 5s
filterwarnings =
    ignore::DeprecationWarning
