{
    "version": 2,
    "buildCommand": "npm run build && mkdir -p .vercel-output && cp -r dist/frontend/browser/* .vercel-output/",
    "outputDirectory": ".vercel-output",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}