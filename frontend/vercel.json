{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "dist/frontend",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}